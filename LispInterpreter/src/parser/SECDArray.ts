

export class SECDArray extends Array< number | string | boolean| SECDArray>{/*
    public popn(cnt: number): SECDArray{
        let result: SECDArray = new SECDArray();
        for(let i = 0; i < cnt; i ++){
            result.push(this.pop());
        }
        return result;
    }*/
/*
    public popArray(): SECDArray{
        let x = this.pop()
        if(Array.isArray(x))
            return x
        return null
    }*/

}